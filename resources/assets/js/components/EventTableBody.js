import React, { Component } from 'react';
import ReactDOM from 'react-dom';

export default class EventTableBody extends Component {
    render() {
        return (
            
        );
    }
}

if (document.getElementById('event-t-body')) {
    ReactDOM.render(<Example />, document.getElementById('event-t-body'));
}
